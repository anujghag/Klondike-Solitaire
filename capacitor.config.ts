import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.premium.klondike',
  appName: 'Klondike Solitaire Premium',
  webDir: 'dist'
};

export default config;
